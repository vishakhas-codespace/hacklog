 open source , high level language, portable
developed by GUIDO VAN ROSSUM
A Variable is a name given to a memory loccation in a program
name="selene"
age=20
price=25.99
rules- Identifiers can be combination of uppercase and lowercase letters digits or an _
so myVariable Variable_1 
2. an identifier cannot start with a digit variable1 is valid 1variable is not valid
3.we cannot use special symbols like !,#,@,%,$ etc in our identifier
identifier can be of any length
Data types-integers string float boolean means True and False, none-a=none
keywords are reserved words in python
and , as , True, False, assert, continue, break, None, if, else etc
python is case sensitive
Types of Tokens
Punctuators are symbols to organize sentence structure in programming
() {} @ [] # etc
-= += *= /= //=  =
PYTHON IS A TYPED LANGUAGE - IMPLICIT interms of Variable declaration koi type batana nahi padta
EXPRESSION EXECUTION
String and Numeric values can operate together with *
A,B=2,3
Txt="@"------------- output  when a string is multiplied with a numeric value its thst the string will get repetated that is 6 times @@@@@@
print(2*Txt*3)
String and String can operate with a +
A, B="2", 3
Txt="@"
print((A+Txt)*B)------------2@2@2@(concatination)
Numeric values can operate with all arithmetic operators
A, B=2,3
C=4
print("A+B*C")------------14
Arithmetic expression with integer and float will result in float
A, B=10,5.0 C=A*B
print(C)---------------50.0
result of division operator with two integers will be float
A,B=1,2
C=A/B------------0.5
print(C)
integer division with float and int will give int displayed as float
A,B=1.5,3
C=A//B-----------0.0 0.5
print(C, A/B) -----0.5
      |
      |
      0.0----integer div

      floor gives  closest integer which is lesser than or equal to the float value 
Result of (A||B) is same as floor(A|B)        /
means floor(number) wil give closest integer /

A,B=12,5
C=A//B--------2
print(C)
  
A,B=-12,5
C=A//B-------  -3    
print(C)

Remainder is negative when denominator is negative
A,B=-5,2
C=A%B-----1
print(C)

A,B=5,2
C=A%B ------1   
print(C)

A,B=5,-2
C=A%B----- -1
print(C)

COMMENTS-single line#   multiline """this is a multiline comment"""
 INPUT IN PYTHON- 
 Input() statement is used to accept values (using keyboard) from user
 #string Input
 name=input("name : ")
 #int input
 age=int(input("age : "))
 float input 
 price=float(input("price : "))

CONDITIONAL STATEMENTS            IDENTATION- Proper spacing

if-elif-else(SYNTAX)
if(condition):
    Statement1
elif(condition):
    Statement2
else:
    StatementN
 
light=input("light : ")
if(light == "red"):
    print("stop") 
elif(light == "yellow"):
    print("look")  
elif(light == "green"):
    print ("go")
else:
    print("light is broken")

marks=input("marks : ")
if(marks>=90):
    print("A")
elif(marks>=80 && marks<90):
    print("B")
elif(marks>=70 && marks<80):
    print("C")
else:
    print("D")

Print output for
A=5 & G=M
A=2 & G=F  
#CODE
A=int(input("A : "))
G=input("M/F : ")
if(A==5 And G==M):
   print("eat")
elif(A==2 And G==F)
   print("not eat") 

    



    